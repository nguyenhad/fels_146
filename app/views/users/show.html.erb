<% provide :title, @user.name %>

<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
        <br>
        <%= @user.email %>
      </h1>
    </section>
    <section class="stats">
      <%= render "shared/stats" %>
    </section>
    <section class="activities_view">
      <h4><%= t "views.users.show.activities_history" %></h4>
      <% if @activities.any? %>
        <ul class="activities">
          <%= render @activities %>
        </ul>
      <% end %>
    </section>
  </aside>
  <div class="col-md-8">
    <%= render "follow_form" %>
  </div>
</div>

